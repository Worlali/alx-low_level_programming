#include <stdio.h>
/**
 * main - a program that outputs a line using printf
 * Return: 0 (ZERO)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
